# direnv: `direnv allow` once, then every shell in this repo is configured.
dotenv_if_exists .env
layout_uv 2>/dev/null || source_env_if_exists .venv/bin/activate
export PYTHONPATH="${PWD}:${PYTHONPATH:-}"
